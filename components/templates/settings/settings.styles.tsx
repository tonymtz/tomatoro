import styled from '@emotion/styled'
import { Badge as _Badge } from 'theme-ui'

export const Badge = styled(_Badge)`
  display: flex;
  align-items: center;
  justify-content: center;
  padding-left: 1em;
  padding-right: 1em;
`
