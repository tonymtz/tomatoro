import styled from '@emotion/styled'
import { Close as TuiClose } from 'theme-ui'

export const Frame = styled.div `
  width: 100%;
`

export const Close = styled(TuiClose)`
    cursor: pointer;
    margin-left: auto;
    margin-right: -2px;
`
